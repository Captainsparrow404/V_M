Media Management APIs Documentation
Generated: 2025-05-16 06:34:43 UTC
Generated by: BrainstormerAI

1. List Media
   Endpoint: GET /api/Media_Management/
   Description: Retrieve a list of all media items
   Query Parameters:
   - status: Filter by status
   - category: Filter by category
   - media_type: Filter by media type
   Note: Results are filtered to show only the current user's media

2. Create Media
   Endpoint: POST /api/Media_Management/
   Description: Create a new media item
   Fields automatically set:
   - author: current user
   - created_by: username of current user
   - created_time: current timestamp

3. Retrieve Media
   Endpoint: GET /api/Media_Management/{pk}/
   Description: Get details of a specific media item
   Parameters:
   - pk: integer (Media ID)

4. Update Media
   Endpoint: PUT /api/Media_Management/{pk}/
   Description: Update an existing media item
   Parameters:
   - pk: integer (Media ID)

5. Partial Update Media
   Endpoint: PATCH /api/Media_Management/{pk}/
   Description: Partially update a media item
   Parameters:
   - pk: integer (Media ID)

6. Delete Media
   Endpoint: DELETE /api/Media_Management/{pk}/
   Description: Delete a media item
   Parameters:
   - pk: integer (Media ID)

7. Archive Media
   Endpoint: POST /api/Media_Management/{pk}/archive/
   Description: Archive a media item
   Parameters:
   - pk: integer (Media ID)
   Response includes:
   - status: success/error
   - message: operation result
   - timestamp: operation time

8. Publish Media
   Endpoint: POST /api/Media_Management/{pk}/publish/
   Description: Publish a media item
   Parameters:
   - pk: integer (Media ID)
   Updates:
   - status to 'published'
   - sets published_date to current time

9. Preview Media
   Endpoint: GET /api/Media_Management/{pk}/preview/
   Description: Preview a media item
   Parameters:
   - pk: integer (Media ID)

Notes:
- Authentication is currently commented out but prepared for Token Authentication
- CSRF protection is disabled for archive endpoint
- All list operations are filtered to show only the authenticated user's media
- Media operations include automatic timestamp and user tracking